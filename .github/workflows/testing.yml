name: Testing

on:
  push:
    branches:
      - "*"

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Run
        run: |
          chmod +x run.sh
          ./run.sh

      - name: Post checkup
        run: |
          df
